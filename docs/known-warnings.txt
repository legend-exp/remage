# Each line should contain the regular expression of a known Sphinx warning
# that should be filtered out
.*Duplicate C\+\+ declaration, also defined at.*
.*Document headings start at H3, not H1.*
.*"api/file_view_hierarchy.rst.include": line 11 exceeds the line-length-limit..*
